public class RealCollaborator implements Collaborator {
    public String getData() {
        // Performs some expensive operation or retrieves data from a remote service
        return "Real data";
    }
}
